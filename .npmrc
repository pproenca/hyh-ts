# Workspace settings (per official pnpm docs)
link-workspace-packages=true

# Strictness settings
strict-peer-dependencies=true

# Keep dependencies isolated
shamefully-hoist=false

# Exact versions for reproducibility
save-prefix=""
